---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: maintenanceoperations.kubegres.reactive-tech.io
spec:
  group: kubegres.reactive-tech.io
  names:
    kind: MaintenanceOperation
    listKind: MaintenanceOperationList
    plural: maintenanceoperations
    shortNames:
    - maintop
    singular: maintenanceoperation
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.kubegresRef.name
      name: Kubegres
      type: string
    - jsonPath: .spec.purpose
      name: Purpose
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .spec.expiresAt
      name: Expires
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: MaintenanceOperationSpec declares an approved, expiring planned
              operation.
            properties:
              approvalReference:
                type: string
              expiresAt:
                format: date-time
                type: string
                x-kubernetes-validations:
                - message: expiresAt is immutable
                  rule: self == oldSelf
              kubegresRef:
                properties:
                  name:
                    type: string
                required:
                - name
                type: object
              purpose:
                description: MaintenancePurpose identifies the approved reason for
                  a planned node operation.
                enum:
                - node-upgrade
                - node-maintenance
                - node-rotation
                type: string
                x-kubernetes-validations:
                - message: purpose is immutable
                  rule: self == oldSelf
              requesterIdentity:
                description: |-
                  RequesterIdentity preserves the human or workflow identity behind the
                  approved service-account request.
                type: string
              safety:
                default: {}
                description: |-
                  MaintenanceSafetyPolicy holds the promotion gate thresholds. Defaults are the
                  approved 5-second / 60-second / 15-minute values.
                properties:
                  maxReplayLagSeconds:
                    default: 5
                    format: int32
                    minimum: 0
                    type: integer
                  relocationDeadlineSeconds:
                    default: 900
                    format: int32
                    minimum: 0
                    type: integer
                  stableForSeconds:
                    default: 60
                    format: int32
                    minimum: 0
                    type: integer
                type: object
              targetNode:
                properties:
                  name:
                    type: string
                  uid:
                    description: UID is immutable and is the authoritative target
                      identity.
                    minLength: 1
                    type: string
                    x-kubernetes-validations:
                    - message: targetNode.uid is immutable
                      rule: self == oldSelf
                required:
                - uid
                type: object
            required:
            - expiresAt
            - kubegresRef
            - purpose
            - targetNode
            type: object
          status:
            properties:
              classification:
                type: string
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              desiredReplicas:
                format: int32
                type: integer
              history:
                items:
                  properties:
                    at:
                      format: date-time
                      type: string
                    evidence:
                      additionalProperties:
                        type: string
                      type: object
                    message:
                      type: string
                    phase:
                      type: string
                  required:
                  - at
                  - phase
                  type: object
                type: array
              observedKubegresGeneration:
                format: int64
                type: integer
              operationID:
                type: string
              phase:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
